# モノレポ環境変数の索引（秘密は書かない。詳細は各 README 参照）
# 実装計画: docs/実装計画.md §4

# --- frontend（frontend/.env.local または Vercel）---
# NEXT_PUBLIC_API_BASE_URL=
# COST_ADMIN_BEARER / COST_SYNC_*（コスト自動取得）はサーバ専用 → frontend/.env.example / README.md

# --- backend（backend/.env または Railway）---
# AWS_REGION=
# BEDROCK_MODEL_ID=
# → backend/.env.example / backend/README.md
